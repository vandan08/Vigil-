route:
  receiver: vigil
  group_by: ["alertname", "service"]
  group_wait: 5s
  group_interval: 30s
  repeat_interval: 5m

receivers:
  - name: vigil
    webhook_configs:
      - url: http://vigil:8080/webhooks/alertmanager
        send_resolved: true
